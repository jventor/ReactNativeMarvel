import { StyleSheet } from 'react-native'
import { main } from '../commons/colors'

export default StyleSheet.create({
  navigationBar: { backgroundColor: main },
  navigationTitle: { color: 'white', fontWeight: 'bold' }
})
